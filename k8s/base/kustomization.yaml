apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - redis.yaml
  - web.yaml
  - celery.yaml
  - ingress.yaml

# Простые метки без лишних опций
commonLabels:
  app: smartmeeting
  managed-by: kustomize
