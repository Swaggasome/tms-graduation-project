apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      component: celery-worker
  template:
    metadata:
      labels:
        component: celery-worker
    spec:
      containers:
        - name: celery
          env:
          - name: CR_REGISTRY_ID
            valueFrom:
              secretKeyRef:
                name: secrets-project
                key: CR_REGISTRY_ID
          image: cr.yandex/"$(CR_REGISTRY_ID)"/smartmeeting:dev
          imagePullPolicy: IfNotPresent
          command: ["celery", "-A", "smartmeeting", "worker", "-l", "info"]
          envFrom:
            - configMapRef:
                name: smartmeeting-config
            - secretRef:
                name: secrets-project
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "384Mi"
              cpu: "300m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-beat
spec:
  replicas: 1
  selector:
    matchLabels:
      component: celery-beat
  template:
    metadata:
      labels:
        component: celery-beat
    spec:
      containers:
        - name: celery-beat
          env:
          - name: CR_REGISTRY_ID
            valueFrom:
              secretKeyRef:
                name: secrets-project
                key: CR_REGISTRY_ID
          image: cr.yandex/"$(CR_REGISTRY_ID)"/smartmeeting:dev
          imagePullPolicy: IfNotPresent
          command:
            - "celery"
            - "-A"
            - "smartmeeting"
            - "beat"
            - "-l"
            - "info"
            - "--scheduler"
            - "django_celery_beat.schedulers:DatabaseScheduler"
          envFrom:
            - configMapRef:
                name: smartmeeting-config
            - secretRef:
                name: secrets-project
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
