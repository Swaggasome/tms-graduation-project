apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: smartmeeting-ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  env:
  - name: HOST
    valueFrom:
      secretKeyRef:
        name: secrets-project
        key: HOST
  tls:
    - hosts:
        - smartmeeting.$(HOST)
      secretName: smartmeeting-tls
  rules:
    - host: smartmeeting.$(HOST)
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 8000
